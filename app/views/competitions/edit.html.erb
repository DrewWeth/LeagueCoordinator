<h1>Editing competition</h1>
<div class="row">
  <div class="col-md-5">
      <%= render 'form' %>
  </div>
  <div class="col-md-7">
    <h4>Current Teams</h4>
    <hr>
    <ul>

    <% @teams.each do |t| %>
      <li><%= link_to t.name, t %> <label class="badge"><%= t.count %></label> <%= link_to 'Edit', edit_team_path(t) %>
</li>
    <% end %>
    </ul>
  </div>
</div>

<%= link_to 'Show', @competition %> |
<%= link_to 'Back', competitions_path %>

<br>
<br><br>
<br><%= link_to 'Destroy', competition_url(@competition), method: :delete, class:"btn btn-danger", data: { confirm: 'Are you sure?' } %>
<br>
<br>
